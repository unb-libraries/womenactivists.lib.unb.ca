// Shared by the directory filter and the `activists` content schema, so the
// UI options and the validated values cannot drift apart.
export const provinces = [
  'New Brunswick',
  'Newfoundland and Labrador',
  'Nova Scotia',
  'Prince Edward Island',
] as const
